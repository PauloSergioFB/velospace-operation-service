# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: 'Build and deploy JAR app to Azure Web App: velospace-operation-service'

on:
  push:
    branches:
    - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Set up Java version
      uses: actions/setup-java@v1
      with:
        java-version: '21'
    
    - name: Build with Maven
      run: mvn clean install -DskipTests
      env:
        JWT_SECRET: ${{ secrets.JWT_SECRET }}

        DB_URL: ${{ secrets.DB_URL }}
        DB_USERNAME: ${{ secrets.DB_USERNAME }}
        DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        
        ACTIVEMQ_BROKER_URL: ${{ secrets.ACTIVEMQ_BROKER_URL }}
        ACTIVEMQ_USER: ${{ secrets.ACTIVEMQ_USER }}
        ACTIVEMQ_PASSWORD: ${{ secrets.ACTIVEMQ_PASSWORD }}
        
        MONGODB_URI: ${{ secrets.MONGODB_URI }}
        
        DELIVERY_API_URL: ${{ secrets.DELIVERY_API_URL }}
        
        INTERNAL_API_KEY: ${{ secrets.INTERNAL_API_KEY }}

    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v2
      with: 
        app-name: 'velospace-operation-service'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_70a3a6e736514fb8a963eef3a7d58b82 }}
        package: '${{ github.workspace }}/VeloSpace/target/*.jar'
